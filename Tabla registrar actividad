CREATE TABLE connection_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_name VARCHAR(50),
    host_name VARCHAR(50),
    operation VARCHAR(50), -- Ejemplo: INSERT, UPDATE
    execution_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
